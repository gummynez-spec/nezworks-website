/* ============ RELAX — placeholder, sidebar/build-up comes next ============ */
(() => {
  const stage = document.getElementById('relaxStage');
  if (!stage) return;

  /* single-screen, pure black stage. New interactions will hook in here. */
})();